FROM llama3.2:3b

# Work around the local Metal backend crash by forcing CPU inference.
PARAMETER num_gpu 0

# Open WebUI 0.11.x can exceed the base model's 4K runtime default before the
# user's first message, even when no tools are selected.
PARAMETER num_ctx 8192
